include "/etc/bind/${domain_name}.key";

zone "${domain_name}" IN {
  type primary;
  file "/var/lib/bind/zone.${domain_name}";
  allow-transfer { key "${domain_name}"; };
  update-policy {
    grant devbox self .;
    grant devbox zonesub ANY;
  };
};
