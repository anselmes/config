---
apiVersion: k0sctl.k0sproject.io/v1beta1
kind: Cluster
metadata:
  name: kubernetes
spec:
  hosts:
    - role: controller+worker
      noTaints: true
      # privateAddress: 127.0.0.1
      # privateInterface: eth0
      ssh:
        address: 127.0.0.1
        keyPath: ~/.ssh/id_ed25519
        port: 22
        user: ubuntu
      # installFlags:
      #   - --disable-components=metrics-server,konnectivity-server,kube-proxy
      #   - --enable-cloud-provider
      #   - --kubelet-extra-args=--cgroup-driver=systemd
      #   - --kubelet-extra-args=--cloud-provider=external
      #   - --labels=openebs.io/nodeid=$(hostname)
      #   - --labels=openstack-compute-node=enabled
      #   - --labels=openstack-control-plane=enabled
      #   - --labels=openstack-gateway=enabled
      #   - --labels=openvswitch=enabled
      # files:
      #   - # audit policy
      #     name: audit
      #     dst: /var/lib/k0s/audit-policy.yaml
      #     src: ./config/audit-policy.yaml
      #   - # containerd patch
      #     name: containerd
      #     dst: /etc/k0s/containerd.d/systemd.toml
      #     src: ./config/containerd.toml
      #   - # custom resources definitions
      #     name: crds
      #     dstDir: /var/lib/k0s/manifests/crds
      #     src: ./crds
      #   - # cilium bgp secret
      #     name: bgp-cred
      #     dstDir: /var/lib/k0s/manifests/secrets
      #     data: |
      #       apiVersion: v1
      #       kind: Secret
      #       metadata:
      #         name: bgp-cred
      #         namespace: kube-system
      #       data:
      #         password: ""
      # hooks:
      #   apply:
      #     before:
      #       - echo "Preparing $(hostname) control+worker node"
      #       - sudo mount --make-rshared /
      #     after:
      #       - echo "$(findmnt -o TARGET,PROPAGATION /)"
      #       - echo "$(findmnt -o TARGET,PROPAGATION /sys)"
      #       - echo "$(hostname) control+worker node ready"
  k0s:
    version: v1.35.4+k0s.0
    config: {}
